<%= content_tag :div, id: "item-#{item.id}" do%>
<%= link_to "", [item.user, item], method: :delete, remote: true, class: 'glyphicon glyphicon-ok'%>
<%= item.name %>
<%= pluralize(item.days_left, "days left")%> 
<% end %>

